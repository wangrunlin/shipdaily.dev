---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="社区动态 - shipdaily.dev"
  description="查看 ShipDaily 社区的最新动态，了解其他开发者的每日 Ship 进展。加入 #shipdaily 挑战，与志同道合的开发者一起成长。"
>
  <!-- Hero Section -->
  <section class="bg-white border-b border-gray-100" aria-labelledby="community-hero-heading">
    <div class="max-w-6xl mx-auto py-20 px-4 sm:py-28 sm:px-6 lg:px-8">
      <div class="text-center">
        <div class="w-16 h-16 bg-gray-100 rounded-2xl flex items-center justify-center text-2xl mx-auto mb-6" aria-hidden="true">
          🌟
        </div>
        <h1 id="community-hero-heading" class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">
          社区动态
        </h1>
        <p class="mt-6 text-lg leading-8 text-gray-600 max-w-2xl mx-auto">
          看看其他开发者的 #shipdaily 进展，获取灵感和动力
        </p>
        <div class="mt-10">
          <a
            href="https://twitter.com/hashtag/shipdaily"
            target="_blank"
            class="inline-flex items-center px-6 py-3 text-sm font-semibold text-white bg-gray-900 rounded-lg hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors duration-200 shadow-sm"
          >
            在 Twitter 查看更多
            <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Community Stats -->
  <section class="py-16 bg-gray-50" aria-labelledby="stats-heading">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 id="stats-heading" class="text-2xl font-bold text-gray-900">社区统计</h2>
        <div class="mt-2 flex justify-center items-center space-x-2">
          <div class="w-2 h-2 bg-orange-400 rounded-full"></div>
          <p class="text-sm text-gray-600">演示数据</p>
        </div>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white rounded-xl border border-gray-200 p-6 text-center hover:shadow-md transition-shadow duration-200">
          <div class="text-2xl font-bold text-gray-900 mb-2" id="active-members">156+</div>
          <p class="text-sm text-gray-600">活跃成员</p>
        </div>
        <div class="bg-white rounded-xl border border-gray-200 p-6 text-center hover:shadow-md transition-shadow duration-200">
          <div class="text-2xl font-bold text-gray-900 mb-2" id="total-ships">2,847</div>
          <p class="text-sm text-gray-600">总 Ship 次数</p>
        </div>
        <div class="bg-white rounded-xl border border-gray-200 p-6 text-center hover:shadow-md transition-shadow duration-200">
          <div class="text-2xl font-bold text-gray-900 mb-2" id="longest-streak">42天</div>
          <p class="text-sm text-gray-600">最长连击</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Recent Activity -->
  <section class="py-16 bg-white" aria-labelledby="activity-heading">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 id="activity-heading" class="text-2xl font-bold text-gray-900">最近动态</h2>
        <p class="mt-2 text-gray-600">
          社区成员的最新 #shipdaily 分享
        </p>
        <div class="mt-4 flex justify-center items-center space-x-2">
          <div class="w-2 h-2 bg-orange-400 rounded-full"></div>
          <p class="text-sm text-gray-500">以下为演示数据</p>
        </div>
      </div>

      <!-- Activity Feed -->
      <div class="max-w-4xl mx-auto">
        <div class="space-y-6" id="activity-feed">
          <!-- Loading state -->
          <div class="text-center py-8">
            <div class="inline-block animate-spin rounded-full h-6 w-6 border-b-2 border-gray-400"></div>
            <p class="mt-2 text-gray-500 text-sm">加载社区动态中...</p>
          </div>
        </div>

        <!-- Load More Button -->
        <div class="text-center mt-8">
          <button 
            id="load-more" 
            class="inline-flex items-center px-6 py-3 text-sm font-semibold text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors duration-200"
            style="display: none;"
          >
            加载更多动态
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- How to Participate -->
  <section class="py-16 bg-gray-50" aria-labelledby="participate-heading">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 id="participate-heading" class="text-2xl font-bold text-gray-900 mb-8">
        如何参与社区？
      </h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">
        <div class="bg-white rounded-xl border border-gray-200 p-6 hover:shadow-md transition-shadow duration-200">
          <div class="text-3xl mb-4" aria-hidden="true">📱</div>
          <h3 class="text-lg font-semibold text-gray-900 mb-3">分享你的进展</h3>
          <p class="text-gray-600 text-sm">
            在 Twitter 上使用 #shipdaily 标签分享你的每日编程进展。
            可以包括学到的新知识、完成的功能、或遇到的有趣问题。
          </p>
        </div>

        <div class="bg-white rounded-xl border border-gray-200 p-6 hover:shadow-md transition-shadow duration-200">
          <div class="text-3xl mb-4" aria-hidden="true">💬</div>
          <h3 class="text-lg font-semibold text-gray-900 mb-3">互动支持</h3>
          <p class="text-gray-600 text-sm">
            点赞、评论和转发其他开发者的 #shipdaily 动态。
            相互鼓励和学习，建立有意义的开发者关系。
          </p>
        </div>
      </div>

      <div class="bg-white rounded-xl border border-gray-200 p-8">
        <h3 class="text-lg font-bold text-gray-900 mb-4">分享模板</h3>
        <div class="bg-gray-50 rounded-lg p-6">
          <p class="text-sm text-gray-600 mb-4">复制并自定义这个模板：</p>
          <div class="bg-gray-100 rounded-lg p-4 font-mono text-sm text-gray-800">
            🚢 #shipdaily Day X<br/>
            📝 今天学习/完成了：[具体内容]<br/>
            💡 学到的新知识：[关键收获]<br/>
            🔗 项目链接：[可选]<br/>
            #coding #buildinpublic #学习
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Mock data for community activity
  const mockActivities = [
    {
      id: 1,
      user: "张三",
      username: "@zhangsan_dev",
      avatar: "👨‍💻",
      content: "🚢 #shipdaily Day 15\n📝 今天完成了用户认证系统的后端 API\n💡 学会了 JWT 的最佳实践\n🔥 连续 15 天！",
      timestamp: "2小时前",
      likes: 12,
      retweets: 3
    },
    {
      id: 2,
      user: "李四",
      username: "@lisi_frontend",
      avatar: "👩‍💻",
      content: "🚢 #shipdaily Day 8\n📝 用 React 重构了旧组件\n💡 发现了性能优化的几个技巧\n📊 页面加载速度提升了 40%",
      timestamp: "4小时前",
      likes: 8,
      retweets: 2
    },
    {
      id: 3,
      user: "王五",
      username: "@wangwu_data",
      avatar: "🧑‍🔬",
      content: "🚢 #shipdaily Day 23\n📝 完成了数据可视化仪表板\n💡 学习了 D3.js 的高级用法\n🎨 效果比预期的还要好！",
      timestamp: "6小时前",
      likes: 15,
      retweets: 5
    },
    {
      id: 4,
      user: "赵六",
      username: "@zhaoliu_mobile",
      avatar: "📱",
      content: "🚢 #shipdaily Day 31\n📝 发布了第一个 Flutter 应用的 beta 版\n💡 移动开发比想象中更有趣\n🎉 一个月的坚持终于有成果了！",
      timestamp: "8小时前",
      likes: 25,
      retweets: 8
    },
    {
      id: 5,
      user: "钱七",
      username: "@qianqi_backend",
      avatar: "⚙️",
      content: "🚢 #shipdaily Day 12\n📝 优化了数据库查询性能\n💡 索引的重要性不能忽视\n⚡ 响应时间从 2s 降到了 200ms",
      timestamp: "10小时前",
      likes: 18,
      retweets: 6
    },
    {
      id: 6,
      user: "孙八",
      username: "@sunba_devops",
      avatar: "🚀",
      content: "🚢 #shipdaily Day 45\n📝 搭建了完整的 CI/CD 流水线\n💡 自动化部署真的能节省很多时间\n🔧 团队开发效率提升明显",
      timestamp: "12小时前",
      likes: 22,
      retweets: 7
    }
  ];

  let currentActivityIndex = 0;
  let isLoading = false;

  function createActivityCard(activity: any) {
    return `
      <div class="bg-white rounded-xl border border-gray-200 p-6 hover:shadow-md transition-shadow duration-300">
        <div class="flex items-start space-x-4">
          <div class="flex-shrink-0">
            <div class="w-10 h-10 bg-gray-100 rounded-full flex items-center justify-center text-lg">
              ${activity.avatar}
            </div>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <h3 class="text-sm font-semibold text-gray-900">${activity.user}</h3>
              <span class="text-sm text-gray-500">${activity.username}</span>
              <span class="text-sm text-gray-500">·</span>
              <span class="text-sm text-gray-500">${activity.timestamp}</span>
            </div>
            <p class="text-gray-700 whitespace-pre-line mb-4 text-sm">${activity.content}</p>
            <div class="flex items-center space-x-6 text-gray-500">
              <button class="flex items-center space-x-1 hover:text-gray-700 transition-colors">
                <span>❤️</span>
                <span class="text-sm">${activity.likes}</span>
              </button>
              <button class="flex items-center space-x-1 hover:text-gray-700 transition-colors">
                <span>🔁</span>
                <span class="text-sm">${activity.retweets}</span>
              </button>
              <button class="text-sm hover:text-gray-700 transition-colors">
                回复
              </button>
            </div>
          </div>
        </div>
      </div>
    `;
  }

  function loadInitialActivities() {
    const activityFeed = document.getElementById('activity-feed');
    const loadMoreBtn = document.getElementById('load-more');
    
    if (!activityFeed) return;

    // Show initial activities
    const initialActivities = mockActivities.slice(0, 3);
    activityFeed.innerHTML = initialActivities.map(createActivityCard).join('');
    currentActivityIndex = 3;

    // Show load more button if there are more activities
    if (currentActivityIndex < mockActivities.length && loadMoreBtn) {
      loadMoreBtn.style.display = 'inline-flex';
    }
  }

  function loadMoreActivities() {
    const activityFeed = document.getElementById('activity-feed');
    const loadMoreBtn = document.getElementById('load-more');
    
    if (!activityFeed || isLoading) return;

    isLoading = true;
    if (loadMoreBtn) {
      loadMoreBtn.innerHTML = `
        <div class="inline-block animate-spin rounded-full h-4 w-4 border-b-2 border-gray-600 mr-2"></div>
        加载中...
      `;
    }

    // Simulate loading delay
    setTimeout(() => {
      const remainingActivities = mockActivities.slice(currentActivityIndex, currentActivityIndex + 3);
      
      if (remainingActivities.length > 0) {
        activityFeed.insertAdjacentHTML('beforeend', remainingActivities.map(createActivityCard).join(''));
        currentActivityIndex += remainingActivities.length;
      }

      // Hide load more button if no more activities
      if (currentActivityIndex >= mockActivities.length && loadMoreBtn) {
        loadMoreBtn.style.display = 'none';
      } else if (loadMoreBtn) {
        loadMoreBtn.innerHTML = '加载更多动态';
      }

      isLoading = false;
    }, 1000);
  }

  // Initialize when DOM is loaded
  document.addEventListener('DOMContentLoaded', () => {
    loadInitialActivities();
    
    const loadMoreBtn = document.getElementById('load-more');
    if (loadMoreBtn) {
      loadMoreBtn.addEventListener('click', loadMoreActivities);
    }
  });
</script>